conda install torchvision torchaudio cpuonly -c pytorch

conda install torchvision cpuonly -c pytorch

<학습 준비>
Data FIle ====> DataFrame, Numpy(전처리) =====> Tensor =====> Dataset(피처 + 타겟) ====> DataLoader 생성

사용자 정의 데이터셋 만들것..
torch.utils.data.Dataset (미완성이기 때문에 인스턴스를 생성할 수 없음. 추상 클래스)

필수 오버라이딩 메서드

def __init__(self) : 데이터셋 전처리 / 초기화 진행 메서드

def __len__(self) : 데이터셋 길이 즉 샘플 수 반환 메서드

def __getitem__(self) : idx 해당 데이터 1개 반환 메서드


ฟ้า
      DataLoader
1만개 ===> 모델 

6만개 ===> 모델



Dataset

f1 + T
1만개


에포크 : [처음=======끝]

배치 크기 : 전체 데이터를 작은 단위로 나눈 크기, 2의 제곱수 크기 


이터레이션 : 에포크 배치 크기로 계산한 반복횟수 W, b

예 ) 데이터 100개 / 배치 크기 20 / 에포크 10개 ===> 50번 학습.

CLASStorch.utils.data.DataLoader(dataset, batch_size=1, shuffle=None, sampler=None, batch_sampler=None, num_workers=0, collate_fn=None, pin_memory=False, drop_last=False, timeout=0, worker_init_fn=None, multiprocessing_context=None, generator=None, *, prefetch_factor=None, persistent_workers=False, pin_memory_device='')


sampler = 꺼내주는 파라미터
num_worker = 일하는 ...
pin_memory = 


CPU
메모리       GPU