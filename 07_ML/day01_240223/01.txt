#API

Application Programming lnterface
- 창 윈도우 생성 함수
- 버튼 생성 함수


클래스 

1. 구성요소   : 메서드(함수) + 속성/필드(변수)

2. 구성요소종류 

	- 클래스 변수 / 클래스 메서드 : 인스턴스 생성 없이 사용 가능. cls

	- 인스턴스 변수 / 인스턴스 메서드 : 必 인스턴스를 생성해야만 사용 가능. self

Class A :
	def __new__(cls) ▶① self ==> 중복

	def __init__(self) ▶② 재사용



객체 : 

인스턴스 : 


# 오버라이딩 = >必 상속 관계

# 오버로딩 => 함수 이름 동일 &  매개변수 다르게 생성

# 상속 class A(B, C, D) : pass
	- 다중상속.


	(기반)	(기반)
데이터 	-> 규칙패턴 	-> 수식
              방법(알고리즘)   	    모델

머신러닝 프로세스

목표설정 -> 데이터 준비 -> 데이터 학습 - > 모델평가 -> 활용

	  알고리즘
	  지도 / 비지도


◎ 데이터셋 : 정확한 규칙/패턴을 찾기 위해 필요한 데이터 집합

분류 : # 목표/목적에 맞게

- 규칙/ 패턴 찾기 위해 입력되는 데이터 ▶ 입력/문제/피처/특성/속성
- 입력 데이터에 해당하는 정답 데이터 ▶타겟/정답/클래스/라벨

준비물 : 데이터 셋
학습 -> 규칙 패턴 찾기
검증 -> 중간 점검(쪽지시험)
테스트 -> 규칙패턴 검사

		↓

	<학습용 데이터 셋>
데이터			라벨
피처(feature)		클래스
			타겟

모델 :
-> 데이터가 가지는 일정한 규칙 또는 패턴을 수식으로 만든 것

머신러닝 학습 준비
- 지도 학습 데이터셋 : 데이터 + 라벨

<하이퍼 파라미터> : 모델의 성능에 영향을 끼치는 파라미터



getter/setter 메서드

-> 속성값 설정 => setter 메서드 set_속성명
-> 속성값 설정 읽기 => getter메서드 get_속성명